# تقرير مشروع: Quran Ayah Correction Chatbot

## 1. عنوان المشروع

**تصحيح الآيات القرآنية باستخدام معالجة اللغة الطبيعية ونماذج Hugging Face**

**English Title:**  
Quran Ayah Correction Chatbot Using NLP, Hugging Face Embeddings, and Fuzzy Matching

---

## 2. الملخص

يهدف هذا المشروع إلى بناء شات بوت ذكي يساعد المستخدم على الوصول إلى الآية القرآنية الصحيحة عند كتابة آية أو جزء منها بشكل غير دقيق، سواء بسبب حذف التشكيل، أو وجود خطأ إملائي بسيط، أو كتابة جزء قصير من الآية.

يعتمد النظام على تقنيات معالجة اللغة الطبيعية للغة العربية، ونموذج Embeddings من Hugging Face، بالإضافة إلى خوارزميات المطابقة التقريبية Fuzzy Matching. يقوم الشات بوت بالبحث داخل قاعدة بيانات كاملة للقرآن الكريم، ثم يرجع أقرب آية صحيحة مع اسم السورة، ورقم الآية، ونسبة التشابه، وتلاوة صوتية للآية.

تم تصميم النظام بطريقة آمنة؛ حيث لا يقوم الموديل بتوليد نص قرآني جديد، بل يسترجع النص الصحيح فقط من Dataset موثوقة. وهذا مهم جدًا في التعامل مع النص القرآني، لأن أي توليد حر للنص قد يؤدي إلى أخطاء غير مقبولة.

---

## 3. مقدمة

النص القرآني يتميز بدقة عالية في الألفاظ والرسم والتشكيل، ولذلك فإن التعامل معه باستخدام نماذج الذكاء الاصطناعي يجب أن يكون بحذر. في كثير من الأحيان قد يتذكر المستخدم جزءًا من الآية فقط، أو يكتبها بدون تشكيل، أو يخطئ في كلمة مثل:

```text
الحمد لله رب العلمين
```

بينما النص الصحيح هو:

```text
ٱلْحَمْدُ لِلَّهِ رَبِّ ٱلْعَٰلَمِينَ
```

من هنا جاءت فكرة المشروع: بناء شات بوت لا يؤلف ولا يعيد صياغة الآيات، بل يبحث داخل القرآن الكريم كاملًا ويعرض أقرب آية صحيحة للمستخدم.

---

## 4. مشكلة المشروع

تتمثل المشكلة الأساسية في أن المستخدم قد يدخل نصًا غير مطابق تمامًا للنص القرآني، مثل:

- آية بدون تشكيل.
- جزء من آية فقط.
- كلمة بها خطأ إملائي.
- اختلاف في شكل الحروف العربية مثل `أ` و`ا` و`إ` و`ٱ`.
- حذف علامات الوقف أو الرموز.

المطلوب هو بناء نظام يستطيع فهم هذا الإدخال ومقارنته بكل آيات القرآن، ثم إرجاع أقرب آية صحيحة من مصدر موثوق.

---

## 5. أهداف المشروع

يهدف المشروع إلى تحقيق الأهداف التالية:

| الهدف | الوصف |
|---|---|
| تصحيح الآية | إرجاع أقرب آية صحيحة بناءً على إدخال المستخدم |
| دعم النص غير المشكول | قبول الآيات المكتوبة بدون تشكيل |
| دعم الأخطاء البسيطة | التعامل مع أخطاء إملائية محدودة |
| البحث في القرآن كاملًا | استخدام جميع آيات القرآن وعددها 6236 آية |
| منع التوليد الخاطئ | عدم استخدام موديل يولد آيات من نفسه |
| إضافة الصوت | تشغيل تلاوة صوتية للآية المطابقة |
| واجهة سهلة | بناء واجهة شات بوت باستخدام Streamlit |

---

## 6. مصدر البيانات

تم استخدام Dataset كاملة للقرآن الكريم من GitHub:

```text
https://raw.githubusercontent.com/malekverse/quran-dataset/main/quran_dataset.csv
```

تحتوي البيانات على معلومات تفصيلية عن كل آية، مثل:

| العمود | المعنى |
|---|---|
| `surah_no` | رقم السورة |
| `surah_name_ar` | اسم السورة بالعربية |
| `ayah_no_surah` | رقم الآية داخل السورة |
| `ayah_no_quran` | رقم الآية في القرآن كاملًا |
| `ayah_ar` | نص الآية بالعربية |
| `ayah_en` | ترجمة إنجليزية للآية |

بعد تحميل الملف، وجد أن عدد الصفوف يساوي:

```text
12472 rows
```

لكن عدد آيات القرآن هو:

```text
6236 ayahs
```

لذلك تم حذف التكرار باستخدام العمود:

```text
ayah_no_quran
```

وبذلك أصبحت البيانات المستخدمة فعليًا تحتوي على 6236 آية فقط.

---

## 7. تجهيز النص العربي

تم إنشاء ملف خاص بتجهيز النص:

```text
preprocessing.py
```

ويحتوي على الدالة:

```python
normalize_arabic(text)
```

وظيفة هذه الدالة هي تحويل النص العربي إلى شكل مناسب للمقارنة، بدون تغيير النص الأصلي المخزن في قاعدة البيانات.

### خطوات التنظيف

| العملية | المثال |
|---|---|
| حذف التشكيل | `ٱلْحَمْدُ` تصبح `الحمد` |
| توحيد الألف | `أ`، `إ`، `آ`، `ٱ` تصبح `ا` |
| توحيد الياء | `ى` تصبح `ي` |
| إزالة المد | `ــــ` يتم حذفه |
| حذف الرموز | علامات الوقف والرموز غير المهمة |
| تنظيم المسافات | حذف المسافات الزائدة |

### مثال

```text
النص الأصلي:
ٱلْحَمْدُ لِلَّهِ رَبِّ ٱلْعَٰلَمِينَ

بعد التنظيف:
الحمد لله رب العالمين
```

هذا يسمح للنظام بمقارنة إدخال المستخدم مع الآيات حتى لو لم يكتب التشكيل.

---

## 8. النموذج المستخدم من Hugging Face

تم استخدام نموذج Sentence Embeddings من Hugging Face:

```text
sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
```

النموذج يقوم بتحويل النصوص إلى متجهات رقمية Vectors. كل آية في القرآن يتم تحويلها إلى Vector، وكذلك إدخال المستخدم يتم تحويله إلى Vector، ثم تتم المقارنة بينهما.

### لماذا لم نستخدم موديل توليد نص؟

لم يتم استخدام موديل Generative AI لتوليد الآية، لأن النص القرآني يحتاج دقة مطلقة. لذلك تم استخدام الموديل فقط في عملية Retrieval، أي البحث والاسترجاع.

بمعنى:

```text
الموديل لا يكتب آية جديدة
الموديل يساعد فقط في العثور على أقرب آية موجودة في الداتا
```

وهذا يجعل النظام أكثر أمانًا وملاءمة لطبيعة المشروع.

---

## 9. آلية البحث والتصحيح

في البداية كان النظام يعتمد على نتائج الموديل فقط، لكن ظهر أن هذا قد يسبب مشكلة عند كتابة جزء قصير من الآية؛ لأن الآية الصحيحة قد لا تظهر ضمن أول النتائج التي يقترحها الموديل.

لذلك تم تحسين النظام ليستخدم طريقتين معًا:

### 9.1 البحث الدلالي Semantic Search

يستخدم Hugging Face model لتحويل النص إلى Embedding ثم حساب التشابه بين إدخال المستخدم وكل آية.

### 9.2 المطابقة التقريبية Fuzzy Matching

يستخدم النظام مكتبة:

```text
RapidFuzz
```

لمقارنة إدخال المستخدم نصيًا مع كل آيات القرآن. هذه الطريقة مهمة جدًا عندما يكون المستخدم كتب جزءًا من الآية أو أخطأ في حرف.

### 9.3 أولوية المطابقة الحرفية

إذا كان النص الذي كتبه المستخدم موجودًا مباشرة داخل آية، يتم إعطاء هذه الآية أولوية أعلى.

مثال:

```text
Input:
صم بكم عمي فهم
```

النتيجة الصحيحة:

```text
صُمٌّۢ بُكْمٌ عُمْىٌۭ فَهُمْ لَا يَرْجِعُونَ
سورة البقرة - آية 18
```

### المعادلة العامة للترتيب

يعتمد ترتيب النتائج على:

```text
Semantic Score
+
Fuzzy Score
+
Exact Substring Priority
```

وبذلك يحصل النظام على توازن بين الفهم الدلالي والدقة النصية.

---

## 10. Workflow النظام

```text
المستخدم يكتب آية أو جزءًا منها
        ↓
تنظيف النص العربي
        ↓
تحويل الإدخال إلى Embedding
        ↓
مقارنته مع Embeddings آيات القرآن
        ↓
تشغيل RapidFuzz على كل الآيات
        ↓
دمج النتائج وترتيبها
        ↓
عرض أفضل آية مطابقة
        ↓
عرض السورة ورقم الآية ونسبة التشابه
        ↓
تشغيل الصوت الخاص بالآية
```

---

## 11. بنية المشروع

```text
quran-chatbot/
│
├── app.py
├── matcher.py
├── preprocessing.py
├── requirements.txt
├── README.md
├── REPORT.md
└── data/
    └── quran.csv
```

### شرح الملفات

| الملف | الوظيفة |
|---|---|
| `app.py` | واجهة الشات بوت باستخدام Streamlit |
| `matcher.py` | تحميل الموديل، بناء Embeddings، وتنفيذ البحث |
| `preprocessing.py` | تنظيف وتوحيد النص العربي |
| `requirements.txt` | المكتبات المطلوبة |
| `data/quran.csv` | قاعدة بيانات القرآن |
| `README.md` | تعليمات التشغيل |
| `REPORT.md` | تقرير المشروع |

---

## 12. جزء الصوت

تمت إضافة ميزة تشغيل الصوت للآية الناتجة.

كل آية في Dataset تحتوي على رقم عام داخل القرآن:

```text
ayah_no_quran
```

هذا الرقم يستخدم لبناء رابط صوتي مباشر من AlQuran Cloud CDN.

شكل الرابط:

```text
https://cdn.islamic.network/quran/audio/128/ar.alafasy/{ayah_no_quran}.mp3
```

مثال:

```text
الآية:
ٱلْحَمْدُ لِلَّهِ رَبِّ ٱلْعَٰلَمِينَ

رقمها العام:
2

رابط الصوت:
https://cdn.islamic.network/quran/audio/128/ar.alafasy/2.mp3
```

ثم يتم عرض الصوت في Streamlit باستخدام:

```python
st.audio(audio_url, format="audio/mp3")
```

القارئ المستخدم حاليًا:

```text
ar.alafasy
```

---

## 13. واجهة المستخدم

تم بناء الواجهة باستخدام Streamlit. تظهر للمستخدم كواجهة شات بسيطة:

1. المستخدم يكتب الآية أو جزءًا منها.
2. البوت يعرض الآية الصحيحة.
3. البوت يعرض اسم السورة ورقم الآية.
4. البوت يعرض نسبة التشابه.
5. البوت يعرض اقتراحات قريبة.
6. البوت يعرض مشغل صوت للآية.

---

## 14. أمثلة الاختبار

### المثال الأول

| العنصر | القيمة |
|---|---|
| الإدخال | `الحمد لله رب العلمين` |
| النتيجة | `ٱلْحَمْدُ لِلَّهِ رَبِّ ٱلْعَٰلَمِينَ` |
| السورة | الفاتحة |
| رقم الآية | 2 |

### المثال الثاني

| العنصر | القيمة |
|---|---|
| الإدخال | `قل هو الله احد` |
| النتيجة | `قُلْ هُوَ ٱللَّهُ أَحَدٌ` |
| السورة | الإخلاص |
| رقم الآية | 1 |

### المثال الثالث

| العنصر | القيمة |
|---|---|
| الإدخال | `صم بكم عمي فهم` |
| النتيجة | `صُمٌّۢ بُكْمٌ عُمْىٌۭ فَهُمْ لَا يَرْجِعُونَ` |
| السورة | البقرة |
| رقم الآية | 18 |

---

## 15. المكتبات المستخدمة

| المكتبة | الاستخدام |
|---|---|
| `streamlit` | بناء واجهة الشات بوت |
| `pandas` | قراءة وتنظيم Dataset |
| `numpy` | العمليات الرقمية وحسابات التشابه |
| `sentence-transformers` | تحميل نموذج Hugging Face |
| `rapidfuzz` | المطابقة التقريبية للنصوص |
| `re` | تنظيف النصوص باستخدام Regular Expressions |

---

## 16. خطوات التشغيل

### تثبيت المكتبات

```powershell
pip install -r requirements.txt
```

### تشغيل التطبيق

```powershell
streamlit run app.py
```

### فتح التطبيق

```text
http://localhost:8501
```

---

## 17. نقاط القوة في المشروع

- يستخدم القرآن كاملًا، وليس عينة صغيرة.
- لا يولد نصًا قرآنيًا، بل يسترجع من Dataset.
- يدعم الكتابة بدون تشكيل.
- يستخدم موديل Hugging Face حقيقي.
- يجمع بين Semantic Search و Fuzzy Matching.
- يعرض اقتراحات قريبة.
- يحتوي على تلاوة صوتية للآية.
- واجهته سهلة ومناسبة للعرض العملي.

---

## 18. القيود الحالية

| القيد | التوضيح |
|---|---|
| أول تشغيل بطيء | بسبب تحميل موديل Hugging Face لأول مرة |
| الصوت يحتاج إنترنت | لأن ملفات الصوت تأتي من CDN |
| المدخلات القصيرة جدًا | قد ينتج عنها أكثر من آية قريبة |
| عدم وجود Highlight | النظام لا يحدد الكلمة الخاطئة حاليًا |
| الاعتماد على Dataset | جودة النتائج تعتمد على جودة البيانات |

---

## 19. التطويرات المستقبلية

يمكن تطوير المشروع مستقبلًا بإضافة:

- إدخال صوتي باستخدام Speech-to-Text.
- اختيار القارئ من قائمة.
- تمييز الكلمة الخاطئة داخل الآية.
- تحميل الصوت Offline.
- إضافة تقييم Metrics مثل Precision@1 و Top-5 Accuracy.
- بناء API باستخدام FastAPI.
- دعم البحث باسم السورة ورقم الآية.
- إضافة Dashboard لإحصائيات البحث.

---

## 20. الخلاصة

نجح المشروع في بناء شات بوت لتصحيح الآيات القرآنية باستخدام تقنيات NLP. يعتمد النظام على تنظيف النص العربي، ونموذج Hugging Face للـ Embeddings، وخوارزميات Fuzzy Matching لتحسين دقة المطابقة.

الميزة الأساسية في المشروع أنه لا يستخدم الذكاء الاصطناعي لتوليد القرآن، بل يستخدمه فقط للبحث والاسترجاع من مصدر موثوق. وهذا يجعل النظام أكثر أمانًا ودقة في التعامل مع النص القرآني.

كما تم دعم المشروع بواجهة تفاعلية وصوت للآية، مما يجعله مناسبًا كفكرة مشروع NLP متكامل يجمع بين النص، الذكاء الاصطناعي، والوسائط الصوتية.
